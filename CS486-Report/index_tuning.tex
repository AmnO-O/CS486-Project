\section{Indexing \& Query Tuning}
\label{sec:index-tuning}

This section summarizes \texttt{15-index-tuning-report} using measurements from
the large Phase 2 dataset.

\subsection{Selected Workloads}
Evaluate the booking-conflict check, the room-finder query, and two selected
analytical reports. State the parameters and data volume used so that the
measurements are reproducible.

\subsection{Indexes and Rationale}
List each added or changed index, its key and included columns, and the query
pattern it supports. Also discuss write and storage costs where relevant.

\subsection{Before-and-After Results}
Compare execution plans, elapsed time, CPU time, logical reads, or the metrics
available in the selected database platform.

\begin{table}[htbp]
    \centering
    \begin{tabularx}{\textwidth}{X X r r X}
        \toprule
        \textbf{Query} & \textbf{Index change} & \textbf{Before} & \textbf{After} & \textbf{Plan change} \\
        \midrule
        % TODO: Insert measured values from 15-index-tuning-report.
        To be completed & To be completed & 0 & 0 & To be completed \\
        \bottomrule
    \end{tabularx}
    \caption{Query-tuning results on the generated dataset}
    \label{tab:index-tuning-results}
\end{table}