# ERD — Campus Space Management System (G05)

**Source:** `docs/entity-registry.md`
**Group:** G05

## Analysis

The Campus Space Management System centers on 7 entities: `Departments`, `Users`, `Spaces`, `Facilities`, `Space_Facilities`, `Bookings`, and `Maintenance`. `Users` is the hub entity, participating in 5 of 9 relationships — as requester, approver, and check-in staff for `Bookings`, and as reporter and assigned staff for `Maintenance`. The M:N between `Spaces` and `Facilities` is resolved via the `Space_Facilities` junction table.

## Entity-Relationship Diagram

```mermaid
erDiagram
    DEPARTMENTS {
        INT department_id PK
        VARCHAR name
        TIMESTAMP created_at
        TIMESTAMP updated_at
    }

    USERS {
        INT user_id PK
        VARCHAR email
        VARCHAR full_name
        VARCHAR phone_number
        VARCHAR role
        INT department_id FK
        VARCHAR account_status
        TIMESTAMP created_at
        TIMESTAMP updated_at
    }

    SPACES {
        INT space_id PK
        VARCHAR space_code
        VARCHAR space_name
        VARCHAR space_type
        VARCHAR building
        VARCHAR floor
        VARCHAR room_number
        INT capacity
        VARCHAR current_status
        VARCHAR usage_policy
        TIMESTAMP created_at
        TIMESTAMP updated_at
    }

    FACILITIES {
        INT facility_id PK
        VARCHAR name
        TIMESTAMP created_at
        TIMESTAMP updated_at
    }

    SPACE_FACILITIES {
        INT space_id FK
        INT facility_id FK
        INT quantity
    }

    BOOKINGS {
        INT booking_id PK
        INT space_id FK
        INT requester_id FK
        TIMESTAMP requested_start_time
        TIMESTAMP requested_end_time
        VARCHAR purpose
        INT expected_participants
        VARCHAR status
        INT approver_id FK
        TIMESTAMP decision_time
        VARCHAR decision_note
        VARCHAR rejection_reason
        TIMESTAMP actual_start_time
        INT checked_in_by FK
        VARCHAR initial_condition
        TIMESTAMP actual_end_time
        VARCHAR final_condition
        VARCHAR usage_notes
        BOOLEAN is_deleted
        TIMESTAMP created_at
        TIMESTAMP updated_at
    }

    MAINTENANCE {
        INT maintenance_id PK
        INT space_id FK
        INT reporter_id FK
        INT assigned_staff_id FK
        VARCHAR problem_description
        TIMESTAMP start_time
        TIMESTAMP completion_time
        VARCHAR status
        VARCHAR result_note
        BOOLEAN is_deleted
        TIMESTAMP created_at
        TIMESTAMP updated_at
    }

    %% R1: Departments -> Users (1:N, Users total — each user belongs to exactly one department)
    DEPARTMENTS ||--o{ USERS : "belongs_to"

    %% R2: Users -> Bookings (requester, 1:N, Bookings total on requester)
    USERS ||--o{ BOOKINGS : "requests"

    %% R3: Users -> Bookings (approver, 1:N, Bookings partial — approver set only after decision)
    USERS |o--o{ BOOKINGS : "approves"

    %% R4: Users -> Bookings (checked-in by, 1:N, Bookings partial — set only at check-in)
    USERS |o--o{ BOOKINGS : "checks_in"

    %% R5: Spaces -> Bookings (1:N, Bookings total on space)
    SPACES ||--o{ BOOKINGS : "hosted_by"

    %% R6: Spaces <-> Facilities (M:N via Space_Facilities, both partial)
    SPACES ||--o{ SPACE_FACILITIES : "contains"
    FACILITIES ||--o{ SPACE_FACILITIES : "assigned_to"

    %% R7: Spaces -> Maintenance (1:N, Maintenance total on space)
    SPACES ||--o{ MAINTENANCE : "reported_for"

    %% R8: Users -> Maintenance (reporter, 1:N, Maintenance total on reporter)
    USERS ||--o{ MAINTENANCE : "reports"

    %% R9: Users -> Maintenance (assigned staff, 1:N, Maintenance partial — assignee may be set later)
    USERS |o--o{ MAINTENANCE : "assigned_to"
```

## Relationship summary

| # | Relationship | Notation | Cardinality | Participation |
|---|---|---|---|---|
| R1 | DEPARTMENTS → USERS | `\|\|--o{` | 1:N | Users total |
| R2 | USERS → BOOKINGS (requester) | `\|\|--o{` | 1:N | Bookings total on requester |
| R3 | USERS → BOOKINGS (approver) | `\|o--o{` | 1:N | Bookings partial |
| R4 | USERS → BOOKINGS (checked-in by) | `\|o--o{` | 1:N | Bookings partial |
| R5 | SPACES → BOOKINGS | `\|\|--o{` | 1:N | Bookings total on space |
| R6a | SPACES → SPACE_FACILITIES | `\|\|--o{` | 1:N | both partial (M:N decomposition) |
| R6b | FACILITIES → SPACE_FACILITIES | `\|\|--o{` | 1:N | both partial (M:N decomposition) |
| R7 | SPACES → MAINTENANCE | `\|\|--o{` | 1:N | Maintenance total on space |
| R8 | USERS → MAINTENANCE (reporter) | `\|\|--o{` | 1:N | Maintenance total on reporter |
| R9 | USERS → MAINTENANCE (assigned staff) | `\|o--o{` | 1:N | Maintenance partial |
