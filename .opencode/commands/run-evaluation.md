---
description: Run the evaluation on database design artifacts using the rubric
---

Use the evaluation rubric in:

`.opencode/evaluations/rubric.md`

Read and evaluate the database design files from:

`outputs/` and the workspace root.

Run the evaluation checklist, score each criterion, identify gaps or problems, and record recommendations and findings in:

`.opencode/evaluations/improvement_logs.md`
