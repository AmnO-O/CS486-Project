# Task 11 — Trajectory (2026-08-10-0901 — review pass on v4.0)

- **Task:** 11 — Concurrency Design (Phase 2)
- **Action:** Review pass on doc v4.0 (post-generation revision requested by user)
- **Revision of:** `logs/trajectory/task11/2026-08-10-0854-trajectory.md`
- **Review findings and fixes (clarification-level only; no design change):**
  1. §4.4 conclusion could be misread — "triggers ... remain as backstop defense-in-depth" now carries a parenthetical clarifying that `trg_bookings_insert_advisory_acknowledgements` is a **primary completeness mechanism (§4.3 layer 1), not a backstop**, and it does not serialize writers (out of the table's scope).
  2. §5.2 criterion 2 ("No schema change — the Task 09 schema stays untouched") scoped explicitly to the **concurrency strategy**: the rev-6 ack trigger is an NR2 completeness mechanism, not a version/optimistic-concurrency object, so the criterion still holds.
  3. The single 4.0 revision-log entry amended with a review-pass sentence (still exactly ONE 4.0 entry per planFix; 3.4 untouched).
  - Confirmed NOT an issue: W1 step-7/8 wording, 3-layer table, consent paragraph, R8/R9 wording, rev-6 pointers all as generated; W1/W2 table rendering audited clean.
  - Cross-file note (flagged, NOT edited — Task 12 approved): Task 12 rev4 header cites "§7.1 step 10 semantics" for the unexpected-error rethrow; in doc v4.0 §7.1 the trigger-error note now sits at step 9 (COMMIT at step 10). The citation remains a doc-internal reference; a future Task 12 touch should point at "§7.1 error-semantics note".
- **Verification:** doc-only revision — no SQL recompile. Full-file audit of changed regions (W1/W2 tables, §4.3/§4.4, §5.2) clean; grep: zero lowercase "report #4", one 4.0 log entry.
- **Evidence:** this trajectory; `outputs/11-concurrency-design-G05.md` v4.0 + review pass
- **Outcome:** v4.0 + review pass complete, awaiting user approval (handshake).