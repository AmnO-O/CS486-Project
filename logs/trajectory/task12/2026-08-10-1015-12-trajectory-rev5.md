# Task 12 Trajectory — rev5 applock return-code fix

Date/time (UTC+7): 2026-08-10 10:15
Author: DB design agent (G05)
Trigger: Task 13 live-run found c01-B red — `usp_booking_instant_submit` threw
52999 ("invalid sp_getapplock return") on the loser-race path.

## What was found

- `sys.sp_getapplock` documents return code `1` as a SUCCESS: "lock granted
  after waiting for other conflicting locks to be released" (0 = granted
  synchronously, -1 = timeout, -2 = cancelled, -3 = deadlock victim,
  -999 = parameter/validation error).
- Task 12 rev4 guarded all four entry points with `IF @lock_rc <> 0` → THROW
  52999. Under real contention, the *loser* session waits up to the 5 s
  timeout, receives the lock with return `1`, and rev4 threw 52999 instead of
  proceeding to its post-lock BR1 re-check and returning the deterministic
  business code 51003.
- Task 11 §5.3 contract lists `0 -> proceed`; return `1` was omitted from the
  table. Fixing on the SQL Server documented contract: both `0` and `1` mean
  "lock acquired" and proceed; only -1/-2/-3 map to 51005/51006/51007; any
  other return (e.g. -999) remains the programmer-error THROW 52999.

## Decision

rev5: in all four entry points
(`usp_booking_instant_submit`, `usp_booking_approve`,
`usp_maintenance_set_impact_level`, `usp_maintenance_report`) change the
defensive guard from `IF @lock_rc <> 0` to `IF @lock_rc NOT IN (0, 1)`, with
comment spelling out the contract. No other code changed; no schema change;
result-code contract (0/51001–51009, 51005/51006 retryable, 51007 restart)
unchanged.

## Evidence

- Compile + smoke (S0–S6d, incl. S1b/S4b trigger proofs): PASS,
  `logs/eval/task12/2026-08-10-1015-12-concurrency-compile-r5.log`
- Idempotent re-run: PASS, `logs/eval/task12/2026-08-10-1015-12-concurrency-idem-r5.log`
- Scratch DB: `CS486_G05_T12R5` (05 → 06 → 10 → 12 ×2)
- Task 13 full suite on rev5 (baseline b01–b10 + controlled c01–c14 + audit +
  teardown): **T13-SUITE: PASS**, `outputs/13-concurrency-tests-G05/results/`
- c01-B now: "PASS c01-B: conflict 51003 (loser side) as designed."
- c01_A/c01_B script comments updated to describe the rev5 behavior.

## Follow-ups

- Task 13 results/SUMMARY.log + FULL_SUITE_RUN.log now green (latest run
  20260810-101557).
- docs/design-decisions.md: this is a Task 12-level implementation fix within
  the approved Task 11 contract (contract table omitted return 1; behavior
  now matches the SQL Server documented contract) — no contradiction; a
  decision-log entry may be appended when the group approves rev5.