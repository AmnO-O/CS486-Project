---
task: "07"
group: G05
evaluated_at: "2026-06-24-1200"
artifact: "outputs/07-query-design-G05.sql"
---

## Artifact score (from rubric.md)

**Command invocation:**
```
generate-query-design --group G05 --student-name "Nguyen Huu Phuoc" --target-users facility_staff
```

| Criterion | Weight | Score (0-5) | Weighted | Evidence |
|---|---|---|---|---|
| Completeness of required outputs | 10% | 5 | 0.50 | File exists at expected path. 5 queries present, each follows the 4-field template (business question, target users, why useful, SQL). Each query annotated with `--student-name` and `--target-users`. |
| Requirement coverage | 20% | 4 | 0.80 | All 5 queries target `facility_staff` correctly. Covers pending approvals (Q1), real-time occupancy (Q2), active maintenance (Q3), check-in eligibility (Q4), session completion reports (Q5). Minor: no explicit no-show or historical audit query. |
| Database design correctness | 20% | 5 | 1.00 | Correct JOIN usage (INNER/LEFT), table aliases, `is_deleted = 0` on soft-deleted tables, proper enum values matching `docs/tech-stack.md`, `NOT IN` avoided, interval logic correct. All queries use parameterized `DECLARE` blocks. |
| Traceability | 15% | 5 | 0.75 | Each query includes `--student-name`, `--target-users`, `--business-question`. Business questions map directly to real facility staff workflows in `req/business-requirement.md` §§ Booking Approval, Check-In, Maintenance. |
| Assumptions and open questions | 10% | 4 | 0.40 | No explicit assumptions section in the file. Queries are self-documenting via the 4-field template. Some queries may return zero rows depending on seed data — no note documenting this possibility. |
| Cross-file consistency | 10% | 5 | 0.50 | Table names (`bookings`, `spaces`, `users`, `maintenances`) match `outputs/05-db-definition-G05.sql` exactly. Column names and enum values consistent with `docs/tech-stack.md` and `docs/entity-registry.md`. |
| SQL Server compatibility | 10% | 5 | 0.50 | T-SQL compatible: `GETDATE()`, `DATEDIFF`, `CAST`, `DATEADD`, `GO` separators. Types match DDL (DATETIME2, NVARCHAR, INT, BIT). No unsupported syntax. |
| Query usefulness | 5% | 5 | 0.25 | All 5 queries serve distinct facility staff operational needs: approval triage, live monitoring, maintenance tracking, no-show detection, end-of-day reporting. Each produces actionable results. |
| **Artifact total** | **100%** | — | **4.70 / 5.00** | |

### File-Level Checks (Task 07)

| Check | Pass? | Evidence |
|---|---|---|
| Query purpose documented | ✅ | Each query has Business question + Why useful |
| Requirement/report mapping | ✅ | Queries map to facility staff workflows in req |
| Correct table/column usage | ✅ | All JOINs use existing columns from DDL |

## Top improvements

1. **Add a no-show detection query** — Facility staff need to identify bookings that passed their end time without check-in (no-show). This would round out the operational coverage.

2. **Document zero-row scenarios** — Add a `-- Note:` comment when a query may return zero rows due to data gaps, so evaluators understand the logic is correct even without matching seed data.

3. **Add booking history query** — A facility staff member may need to look up past bookings for a specific space or requester when investigating disputes or damage reports.
